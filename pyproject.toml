[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "pygoattracker"
version = "0.1.6"
description = "Pure-Python reader, writer, and player for GoatTracker 2 (.SNG) songs"
readme = "README.md"
requires-python = ">=3.11"
license = { file = "LICENSE" }
authors = [{ name = "Josh Bailey" }]
keywords = ["sid", "c64", "goattracker", "tracker", "chiptune", "pyresidfp"]
classifiers = [
    "License :: OSI Approved :: Apache Software License",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Topic :: Multimedia :: Sound/Audio",
]
dependencies = [
    "pysidtracker>=0.4",
]

[project.optional-dependencies]
# Audio rendering drives an emulated SID via pysidtracker's pyresidfp backend;
# everything else (read/write/play/register logs) is pure stdlib.
audio = ["pysidtracker[audio]>=0.4"]
# Decompiling crunched/relocated .sid files runs their 6502 init in py65;
# direct-load packed .sid files decompile with stdlib only.
sid = ["py65>=1.1"]
dev = [
    "black>=24",
    "pylint>=3",
    "py65>=1.1",
    "pyresidfp>=0.17",
    # numpy accelerates pysidtracker's image anchor scan (pysidtracker[fast]);
    # the HVSC corpus test relies on it to stay well under its time budget.
    "numpy>=1.23",
    "pytest>=7",
    "pytest-cov>=4",
    "pytest-xdist>=3",
]

[project.scripts]
pygoattracker = "pygoattracker.cli:main"

[project.urls]
Homepage = "https://github.com/anarkiwi/pygoattracker"
Source = "https://github.com/anarkiwi/pygoattracker"
Issues = "https://github.com/anarkiwi/pygoattracker/issues"

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
minversion = "7.0"
testpaths = ["tests"]
addopts = "-ra -n auto"

[tool.coverage.run]
branch = true
source = ["pygoattracker"]

[tool.coverage.report]
fail_under = 85
show_missing = true

[tool.black]
line-length = 88
target-version = ["py311"]

[tool.pylint.main]
disable = "all"
enable = [
    "redefined-builtin",
    "unused-import",
    "unused-argument",
    "cell-var-from-loop",
    "unused-variable",
    "unbalanced-tuple-unpacking",
    "stop-iteration-return",
    "reimported",
    "dangerous-default-value",
    "pointless-statement",
    "using-constant-test",
    "raise-missing-from",
]
